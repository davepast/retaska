<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('homepage') }}">reTaska.cz</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            {#<li class="nav-item {{ route ==  'homepage' ? 'active' }}">
                <a class="nav-link" href="{{ path('homepage') }}" {{ route ==  'homepage' ? 'onclick="return false;"' }}>Domů</a>
            </li>#}
            <li class="nav-item {{ route starts with 'list' ? 'active' }}">
                <a class="nav-link" href="{{ path('list') }}" {{ route ==  'list' ? 'onclick="return false;"' }}>Seznam produktů</a>
            </li>
            <li class="nav-item {{ route ==  'contact' ? 'active' }}">
                <a class="nav-link" href="{{ path('contact') }}" {{ route ==  'contact' ? 'onclick="return false;"' }}>Kontakt</a>
            </li>
            <li class="nav-item {{ route starts with  'basket' ? 'active' }}">
                <a class="nav-link" href="{{ path('basket_index') }}" {{ route ==  'basket_index' ? 'onclick="return false;"' }}>Košík</a>
            </li>
        </ul>
        <form action="{{ path('search') }}" class="form-inline my-2 my-lg-0" method="get">
            <ul class="navbar-nav mr-auto">
                <li id="admin" class="nav-item">
                <input class="form-control mr-sm-2" type="search" id="search" name="search" placeholder="Vyhledat" aria-label="Hledat">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Hledat</button>
                </li>
                <li class="nav-item">
                    <a class="nav-link" style="color:gray" href="{{ path('admin_homepage') }}">Administrace</a>
                </li>
            </ul>

        </form>

    </div>

</nav>
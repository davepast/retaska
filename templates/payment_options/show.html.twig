{% extends 'base.html.twig' %}

{% block title %}PaymentOptions{% endblock %}

{% block body %}
    <h1>PaymentOptions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment_option.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ payment_option.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ payment_option.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ payment_option.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('payment_options_index') }}">back to list</a>

    <a href="{{ path('payment_options_edit', {'id': payment_option.id}) }}">edit</a>

    {{ include('payment_options/_delete_form.html.twig') }}
{% endblock %}

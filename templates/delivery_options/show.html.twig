{% extends 'base.html.twig' %}

{% block title %}DeliveryOptions{% endblock %}

{% block body %}
    <h1>DeliveryOptions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ delivery_option.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ delivery_option.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ delivery_option.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ delivery_option.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('delivery_options_index') }}">back to list</a>

    <a href="{{ path('delivery_options_edit', {'id': delivery_option.id}) }}">edit</a>

    {{ include('delivery_options/_delete_form.html.twig') }}
{% endblock %}
